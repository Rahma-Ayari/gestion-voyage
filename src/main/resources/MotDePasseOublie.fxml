<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>
<?import javafx.scene.image.*?>

<StackPane fx:id="rootPane" prefWidth="1200.0" prefHeight="800.0"
           focusTraversable="true"
           xmlns="http://javafx.com/javafx/8"
           xmlns:fx="http://javafx.com/fxml/1"
           fx:controller="Controller.ControllerMotDePasseOublie">

    <!-- Image de fond -->
    <ImageView fitHeight="800.0" fitWidth="1200.0" opacity="0.5"
               pickOnBounds="true" preserveRatio="false">
        <image>
            <Image url="@/images/voyage_bg.jpg" />
        </image>
    </ImageView>

    <!-- Carte principale -->
    <VBox alignment="CENTER" spacing="20.0"
          maxWidth="420.0" maxHeight="420.0"
          style="-fx-background-color: rgba(244, 244, 244, 0.93);
                 -fx-border-color: #FF9800;
                 -fx-border-width: 3;
                 -fx-border-radius: 20;
                 -fx-background-radius: 20;
                 -fx-effect: dropshadow(three-pass-box, rgba(0,0,0,0.3), 10, 0, 0, 0);">

        <padding>
            <Insets bottom="35.0" left="40.0" right="40.0" top="35.0" />
        </padding>

        <!-- Icône -->
        <Label text="🔒"
               style="-fx-font-size: 42px;" />

        <!-- Titre -->
        <Label text="Mot de passe oublié ?"
               textFill="#FF9800" textAlignment="CENTER">
            <font>
                <Font name="Segoe UI Bold" size="24.0" />
            </font>
        </Label>

        <!-- Instruction -->
        <Label text="Saisissez votre adresse email. Nous vous enverrons un code de vérification pour réinitialiser votre mot de passe."
               textFill="#666666" textAlignment="CENTER" wrapText="true">
            <font>
                <Font name="Segoe UI" size="13.0" />
            </font>
        </Label>

        <!-- Champ email -->
        <!--  fx:id="emailRecupField" → correspond à @FXML TextField emailRecupField dans ControllerMotDePasseOublie -->
        <TextField fx:id="emailRecupField"
                   promptText="Votre adresse e-mail"
                   style="-fx-background-radius: 20;
                          -fx-padding: 10;
                          -fx-border-color: #FF9800;
                          -fx-border-radius: 20;
                          -fx-background-color: white;
                          -fx-font-size: 13px;"
                   prefWidth="320.0"/>

        <!-- BOUTON ENVOYER LE CODE →  onAction="#handleSendCode" dans ControllerMotDePasseOublie -->
        <Button text="📨  Envoyer le code"
                onAction="#handleSendCode"
                prefWidth="260.0" prefHeight="44.0"
                style="-fx-background-color: #FF9800;
                       -fx-text-fill: white;
                       -fx-font-weight: bold;
                       -fx-background-radius: 25;
                       -fx-cursor: hand;
                       -fx-font-size: 14px;" />

        <!-- Lien retour Login →  onAction="#showLogin" dans ControllerMotDePasseOublie -->
        <HBox alignment="CENTER" spacing="5">
            <Label text="Vous vous souvenez ?"
                   style="-fx-text-fill: #666; -fx-font-size: 13px;"/>
            <Hyperlink text="Se connecter"
                       onAction="#showLogin"
                       style="-fx-text-fill: #FF9800;
                              -fx-font-weight: bold;
                              -fx-border-color: transparent;
                              -fx-font-size: 13px;" />
        </HBox>

    </VBox>
</StackPane>
